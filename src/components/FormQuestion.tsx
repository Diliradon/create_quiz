import { FormInput } from './FormInput';

export const FormQuestion = ({}) => {
  return (
    <form className="flex max-w-2xl flex-col gap-4 rounded-md border-2 p-4">
      <FormInput />
    </form>
  );
};
